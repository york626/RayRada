# 更新记录

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。每次发布在下方记录更新备注，GitHub Release 里使用同一份说明。

## v4.17 — 2026-09-25

**新增：按应用（进程）的流量统计**

- **流量统计窗口新增「应用流量排行」**（右侧 Top 10 横向条，下行蓝 / 上行橙，可切 **今天 / 30 天 / 1 年**）与 **「按应用明细」页**（应用 / 今天 / 30 天 / 1 年 / 合计，**带搜索框**）；左上「流量趋势」多了**应用下拉**，可以直接看某个应用的逐日曲线。窗口从 780×672 调整为 980×712。
- **数据来源用 ETW**（provider `Microsoft-Windows-Kernel-Network`，GUID `{7DD42A49-5329-4832-8DFD-43D979153A88}`）：内核在每次 TCP/UDP 收发时发事件，**PID 与字节数都在事件里** ⇒ **不抓包、不做「五元组 → PID」映射、不装驱动、不引第三方库**（纯 `advapi32.dll` P/Invoke）⇒ **仍然是单文件 exe**、编译方式不变。
- **存储**：`%APPDATA%\RayRadar\apptraffic\YYYY-MM-DD.dat`（每天一个 TSV：`应用 \t 下行 \t 上行`，只整写当天那份，原子替换；保留约 13 个月）。不引入 SQLite —— 原生 interop DLL 没法像托管 DLL 那样嵌进单文件 exe，而每天每应用一行、一年才 1~2 MB，不需要数据库。
- **口径**（界面状态行也写了）：ETW 是 IP 层、且**纯 ACK 不产生事件** ⇒ 「按应用合计」通常**略小于**卡片里的网卡总量（下载时网卡「发送」会明显大于按应用发送，差值就是 ACK）；回环（127.0.0.1）与链路层头都不计。只统计**本程序运行期间**（与原有总量同口径）。
- **失败优雅降级**：起不了 ETW 会话（非管理员 / 64 位自检不过 / 会话被占）时，窗口状态行写明原因，其余功能一律不受影响。

**踩坑记录（这块坑很深，都写进代码注释了）**

- `EVENT_TRACE_LOGFILE` **必须自己分配非托管内存、整场会话保活**：用 `ref` 传托管结构体时，marshaller 建的临时副本在调用返回后即释放，而 ETW 还在用它 ⇒ **`0xc0000005` 崩溃**（启动约 3 秒必崩）。
- 这个结构体**比按 C 定义手算的 416 字节大**，缓冲必须给足（本实现多留 4KB），否则 ETW 回写越界 ⇒ **`0xC0000409`**。
- **回调偏移别照抄常量**：本机实测 `BufferCallback = 400`、`EventRecordCallback = 424`（网上常见的 368 / 392 整整差 32 字节；照抄的结果是「一条事件都收不到，但也不报错」）。反推办法：把回调指针在 368~448 每个 8 字节槽位各放一个**独立计数器**，跑一次看哪个在涨。
- **内核网络事件的「事件头 PID」恒为 4（System）**，真正的主人在**载荷偏移 0**；读错的话所有流量都会算到「System」头上（实测踩过）。
- 起会话的 `EVENT_TRACE_PROPERTIES` 必须设 `Wnode.Flags = WNODE_FLAG_TRACED_GUID`、`Wnode.ClientContext`、会话 GUID。
- `StartTrace` / `ControlTrace` / `OpenTrace` 在 DLL 里的**真实导出名带 W 后缀**（`StartTraceW`…），`ExactSpelling=true` 时不带后缀会报「找不到入口点」。
- 进程名解析要在**首次见到该 PID 时**做（连接/断开类事件也拿来预热）：ETW 事件比进程退出**晚约 1 秒**到达，拖到落盘时再解析，短命进程只能显示成「PID 1234」。

**已知限制**

- **亚秒级的短命进程**（跑一下就退出的命令行工具）可能显示成 `PID 1234`，原因同上（事件到达时进程已退出，名字查不回来）。
- ETW 会话在程序退出时停止；**只统计 Ray雷达 运行期间**，与原有总量一致。

**自测装置（`tests\`，跑完即删产物）**

- `SimEtw.cs`（**需管理员**）：端到端验采集 —— 起会话、收事件、与网卡计数器对账、打印按事件号分项与每 PID 的解析结果。
- `SimApp.cs`（免管理员）：`AppTraffic` 的聚合 / 排行 / Top-N 合并 / 时间窗过滤 / 逐日序列 / 原子写 / 裁剪，**19 项断言全过**。
- `SimShot.cs`（免管理员）：用合成数据把流量窗口画出来存 PNG（配合 `RAYRADAR_TRAFFIC_DATA` 与 `RAYRADAR_APPTRAFFIC_DIR`）。

## v4.16 — 2026-09-24

**修复（用户反馈「网页打不开了」）**
- **竞彩计算器服务器加看门狗**：每 **30 秒**看一眼，进程意外退出就**自动拉起来**并写一行日志。
  起因：雷达**只在启动时**拉一次服务器，之后就不管了；实测那次 node 被别的程序清理进程树时带走，页面从此一直打不开（浏览器只显示「网页拒绝了您的访问」）。
- 规则三条：① 开关关着 ⇒ 不管；② **用户在设置里点过「停止服务」⇒ 不管**（不跟用户对着干）；③ 已有进程在监听 ⇒ 不管（不去抢别处启动的）。
- 开销：核心判断只读子进程句柄（`proc.HasExited`），**不 spawn 任何进程**，30 秒一次可忽略。

**实测**
- `tests\SimCalc.cs` 新增看门狗用例，**全部通过**：杀掉监听进程 → 确认已停 → `Watchdog()` ⇒ **自动拉起** ✓；再 `Stop()`（等于用户手动停）→ `Watchdog()` ⇒ **保持停止** ✓。
- 真机：重启雷达 ⇒ **4 秒内**自动拉起服务器，`http://192.168.31.101:8000/` 返回 HTTP 200（98,060 字节）；`sentinel.log` 可见「竞彩计算器服务器（看门狗）：已启动：…」。

## v4.15 — 2026-09-24

**新增：把「竞彩计算器」的本地服务器并进雷达管理（用户要求「设置里面搞一个开关」）**
- 设置窗新增一节「**竞彩计算器服务器**」：一个开关 + 网页目录输入框 + 状态行 +「启动服务」「停止服务」两个按钮。
- **开关 = 随雷达一起启动**：雷达本身由计划任务开机自启 ⇒ 勾上就等于**开机自启**，而且**不弹黑窗口**（起子进程用 `CreateNoWindow` + `WindowStyle.Hidden`）。
- **全程免 UAC**：雷达本身就是管理员权限运行，起停子进程不需要任何提权确认。
- 端口固定 **8000**；状态行直接给出手机该开的网址（用 `LanSentinel.LanIp()` 取当前内网 IP，取不到就退回落环）。
- **只发网页、不转发数据**：竞彩计算器的赔率与开奖结果都是**手机浏览器直接向竞彩官网取**（官方接口带 `Access-Control-Allow-Origin: *`），这台服务器只负责把 `index.html` 发给手机。

**实现要点**
- 新增静态类 `CalcServer`：`Dir / ScriptPath / NodeExe / ListenerPids / Running / Url / StateText / Start / Stop / AutoStart`。
- `node.exe` 按「用户目录 `nodejs\` → `Program Files\nodejs\` → `%LOCALAPPDATA%\Programs\nodejs\` → `where node`」顺序找；找不到给可读提示。
- 网页目录留空则默认 `我的文档\DSH常用\竞彩计算器`（可用设置里的输入框改）。
- 停止时**按端口查 PID 再杀**（解析 `netstat -ano`），不会误杀别的 node 进程。
- 目录不存在 / node 缺失 / 端口没起来，各有对应文案，不抛异常、不弹窗打扰。

**实测（2026-09-24）**
- 自测装置 `tests\SimCalc.cs`（**不需要管理员**）**全部通过**：找到 `serve.mjs` / 找到 `node.exe` / 启动后端口监听 / **HTTP 真取首页 78,344 字符且含 `webapi.sporttery.cn`** / 目录填错时给出可读报错且状态行同步说明 / 停止后端口释放。
- 真机端到端：编译后重启雷达 ⇒ **4 秒内自动拉起服务器**，`http://192.168.31.101:8000/` 返回 **HTTP 200**（85,391 字节）。
- 同时**撤掉了此前手工加在「启动」文件夹里的快捷方式** —— 自启改由雷达统一负责，避免两份启动源。

**说明**
- 默认开启（`CalcServer=1`）。在没放这个网页的电脑上勾着也无害：找不到目录时只在 `sentinel.log` 里记一行「未启用」，不报错、不弹窗。

## v4.14 — 2026-09-20

**修复（用户实测反馈：陌生设备已经把网页端打开、雷达还卡住了一会儿才拦到）**
- **拦截速度：约 3 秒 → 0.32 秒（实测）**：
  - 以前扫描跑在**界面线程**上（每秒一次），里面还要起 `netsh` 进程、对每个连接发 `SendARP`（各可能卡几百毫秒到几秒）⇒ 浮窗表现为「卡住没反应」，拦截也被拖慢。
    现在哨兵跑在**独立后台线程**（默认 **500 毫秒**一次），界面线程完全不碰 netsh/ARP。
  - 顺序也修正：**先判 IP 白名单**（白名单设备不再做 ARP）、**先切断连接 + 删转发，最后才查 MAC 写日志**（ARP 慢也不拖慢拦截）。
- **拦截弹窗改为非模态**（`Show()` 而不是 `ShowDialog()`）：弹窗期间**浮窗继续刷新、哨兵继续扫描**；同一时间只保留一个拦截弹窗，不会堆叠。
- **拦截时主动切断对方已建立的 TCP 连接**（`SetTcpEntry` + `MIB_TCP_STATE_DELETE_TCB`），然后再删掉端口转发 ⇒ 正在使用的页面**立刻断线**，而不只是「新连接进不来」。

**实测（2026-09-20 12:52）**：临时关掉「放行本机自测」→ 从本机连接入口 ⇒ **0.32 秒**内完成「识别 + 切断 + 删转发 + 弹窗」，日志两行：
「⚠ 陌生设备连接入口：192.168.31.101 —— 立即阻断」「已自动关闭手机入口并切断其连接（阻断 192.168.31.101，切断 1 条 TCP）」；
同时雷达进程 `Responding=True`、窗口列表里「Ray雷达」与「Ray雷达 · 入口拦截」并存 ⇒ **浮窗没被弹窗冻结**。
另有一项独立实验：删掉端口转发**本身也会切断已建立的连接**（HTTP 401 → 删转发 → 再请求报「你的主机中的软件中止了一个已建立的连接」）。

**说明**
- 「陌生设备为什么能先看到界面」＝ 拦截有约 1 秒窗口 + 那台设备上有**之前测试残留的登录 cookie**；cookie 是客户端凭据，哨兵管不到，建议在测试机上清掉该站点数据。

## v4.13 — 2026-09-20

**新增**
- **入口拦截弹窗直接给两个按钮**（用户 2026-09-20 要求）：「**重开手机入口**」与「**保持关闭**」。
  - 「重开手机入口」**只把入口开回来，不会把任何设备加进白名单** —— 重开之后哨兵照常监控，**陌生设备仍然会被拦掉**。
  - 回车默认键是「**保持关闭**」，避免手快误触重开。
  - 温度报警弹窗不受影响（仍是单个「知道了」）。
- `OpenEntry()` 现在会**清空 60 秒报警冷却**：重开入口后立即重新判定，陌生设备**马上**再被拦一次，不会因为还在冷却期而被放进来。

**说明**
- 按用户明确要求，**没有**加「加白名单并重开」这类按钮：白名单只在设置窗里手动改，避免在报警弹窗上顺手把陌生设备放进来。

## v4.12 — 2026-09-20

**修复**
- **「入口拦截」弹窗里的大字误显示为「温度报警」**（用户 2026-09-20 截图反馈）：弹窗标题栏是对的（`Ray雷达 · 入口拦截`），但里面那行红色大字在 `AlertForm` 里是**写死**的。
  现在 `AlertForm` 增加一个可选参数 `headText`（不传仍是「温度报警」，温度报警的调用点不用改），入口拦截弹窗显示「**⚠ 陌生设备接入**」。
- 入口拦截弹窗的提示文案改为指向新恢复方式：**右键浮窗 → 设置 →「重开手机入口」**（不用管理员确认），并提示用同一节的「加入上次拦截」把自家设备加白。

**说明**
- 真实使用反馈（2026-09-20 12:16 / 12:19）：用户两次用 v4.11 的「重开手机入口」按钮成功恢复入口（**免 UAC，功能验证通过**），
  但自家手机随后仍被拦 —— 原因是白名单里还是手机的**旧 IP / 旧随机 MAC**。⇒ **换手机 MAC 或 IP 后，记得点一次「加入上次拦截」**。

## v4.11 — 2026-09-20

**新增**
- **设置窗口里直接开关「DSH 手机入口」**（在哨兵那一节下方）：
  - 「**重开手机入口**」：自动探测当前内网 IP（优先「有默认网关」的网卡，跳过回环与 169.254）→ 删掉该端口的旧转发 → 重建 `内网IP:3081 → 127.0.0.1:3082` → 防火墙缺规则时补一条（`DSH Web LAN 3081`，仅放行本地子网）。
  - 「**关闭手机入口**」：删掉该端口的所有转发（等于关掉入口）。
  - 状态行实时显示：`已开启：192.168.31.x:3081 → 127.0.0.1:3082` 或 `未开启（手机现在连不上这台电脑）`。
  - **关键点：本程序本来就以管理员运行，所以这两个动作不弹 UAC** —— 被哨兵拦截、或误关了入口之后，右键浮窗进设置即可一键恢复。
  - 若内网网段变了，手机侧可能还会被 DSH 的「信任栅栏」（`trustedHosts`）挡住：按钮的提示会指引用一次 `开放手机入口.cmd`（它同时更新栅栏，改完需重启 DSH）。
- `LanSentinel` 新增 `LanIp()` / `OpenEntry()` / `CloseEntry()` / `StateText()` / `EntryOpen()`。

**说明**
- 自测装置：`tests\SimEntry.cs`（2026-09-20 起随本仓库分发，原在工作区 `_rayradar测试\`；编译命令写在文件头，`/codepage:65001` 防中文乱码；**运行需管理员**，会真的改 netsh portproxy，跑完自动恢复成「开启」）。
  实测 2026-09-20：内网 IP 探测 `192.168.31.101` ✓；关闭 → 转发消失 ✓；重开 → 转发恢复 ✓；重复关闭给出「无需关闭」✓；最终状态 `已开启` ✓。

## v4.10 — 2026-09-20

**新增**
- **DSH 手机入口哨兵**（设置窗口新增一节，**默认开启**）：监控本机 **3081 端口转发**（DSH 手机网页入口）上的连接，
  **白名单之外的设备一连上就**：写日志 → 提示音 → **自动删掉 3081 转发（阻断）** → 弹窗告知（复用温度报警的弹窗）。
  - **对别人无害**：只有本机确实存在 3081 端口转发（＝开了手机入口）时才工作；没有这个转发的电脑，模块什么都不做 —— 所以默认开启是安全的。
  - **白名单**：设置里填**逗号分隔的 IP 或 MAC**（例 `192.168.31.18,82-8B-68-6C-2F-FF`），另有「**加入上次拦截**」按钮可一键把刚被拦的设备加白。
    IP 与 MAC **任一命中即放行** —— 手机用随机 MAC、或 DHCP 换了 IP 都不会误拦。
  - 「放行本机自测连接」开关：默认开（自己在电脑上开网页不会被拦）；想自测拦截效果可临时关掉。
  - **日志**：`%APPDATA%\RayRadar\sentinel.log`（时间 / IP / MAC / 是否阻断）。
  - **实现**：`IPGlobalProperties.GetActiveTcpConnections()` 每秒查一次（**零进程开销**，比外部 PowerShell 版轻得多，实测后者约 158MB 内存 / 0.8% 单核）；
    对端 MAC 用 `SendARP`；阻断用 `netsh interface portproxy delete` —— 本程序本来就以管理员运行，**不需要额外提权、不需要额外计划任务**。
  - 实测（2026-09-20）：先以独立 PowerShell 哨兵跑通同一套逻辑（**真实拦到一次连接**，含提权阻断链路）；
    本版随后接入，编译通过；接入后再做一次**注入式自测**（临时清空白名单 + 关「放行本机自测」→ 从本机连入口）：**检测 → 报警 → 阻断 → 转待机四段全通**，还原设置后白名单设备再连一次**日志零新增**（不误报）。

**说明**
- 本功能用于替代此前单独部署的 PowerShell 哨兵（`~\.dsh\tools\dsh-lan-sentinel\`）；装上本版后那份可以删除，避免两套同时阻断。

## v4.9 — 2026-09-18

**修复**
- **每次启动游戏都误报「CPU 温升过快」**（用户反馈：游戏刚起来温度 +16°C，十几秒后自己降回去）。原因是温升判断「一次测量就报警」，
  区分不了「负载尖峰（升一下就停）」和「液冷故障（持续爬升）」。
  现在改为**两次确认**：第一次测到 20 秒内陡升 ≥ 阈值时**只记下、不报警**，等 **20 秒后复测**——仍在陡升才报警，已回落则静默取消。
  这样既保留 15°C 的灵敏度（真漏液必然持续升温，会触发温升报警或 90°C 绝对阈值），又不会因游戏/编译/解压等瞬时负载误报。
  报警文案改为「CPU 温度持续上升，40 秒内累计上升 XX°C（当前 XX°C）」；设置里该开关下加了说明文字。

## v4.8 — 2026-09-18

**修复**
- **开机误报温升**（用户重启电脑后刚开机就弹「CPU 温度 20 秒内上升 15°C」）：温升判断现在有三重保护——
  ① 程序启动后 **3 分钟预热期内不判温升**（开机阶段温度本身在爬升）；
  ② 当前温度必须 **≥ 45°C**（低温区的正常波动不再算数）；
  ③ 采样间隔超过 8 秒（睡眠唤醒、程序被挂起）就**重新开始积累**，不跨空档比较。
  报警文案也补上了当前温度：`CPU 温度 20 秒内上升 15°C（当前 92°C，阈值 15°C）`
- **报警弹窗文字被截断**（用户截图反馈：底部提示那行超出窗口看不见）：弹窗改为**按内容自动换行并撑高**——
  用 `TextRenderer.MeasureText` 量出换行后的实际高度，逐行排布、窗口高度随之计算，长句（液冷排查提示）完整可见；
  提示行改用灰色小字与正文区分。

## v4.7 — 2026-09-18

**修复**
- 双击温度块后，展开的浮层会被紧随的那次 MouseUp 又打开一次（表现为闪一下仍展开）。现在双击后会抑制该次抬起，双击的净效果是「收起」
- `build.ps1`：`taskkill` 往 stderr 写内容时，在 `$ErrorActionPreference='Stop'` 下会中断脚本；已临时放开该设置，保证「结束旧实例」的兜底链路不会被自己打断

## v4.6 — 2026-09-18

**变更**
- **双击浮窗不再打开设置窗口**（用户反馈误触）：现在**只有右键**打开设置窗口；双击只是收起温度浮层
- `build.ps1` 结束旧实例的顺序加强：`Stop-Process` → `taskkill /F /IM RayRadar.exe` → `schtasks /End`（逐个兜底，避免编译时报「输出文件被占用」）

**说明**
- 从本版起，GitHub Release 的说明**只包含本版本的更新内容**（之前的正文会连带更早版本，已一并修正）；仓库里的 `CHANGELOG.md` 仍然保留完整历史

## v4.5 — 2026-09-18

**流量统计窗口全面重做**（按用户给的 UI/UX 需求）
- **上：总览卡片** —— 「今天 / 最近 30 天 / 最近 1 年」三张卡片，各显示 ↓下行（蓝）、↑上行（橙）、合计
- **中：堆叠柱状图** —— 下行（蓝）在下、上行（橙）在上；Y 轴自动换算 KB / MB / GB 并取整刻度；X 轴为日期（MM-dd）；
  右上角图例；**鼠标悬停显示当天明细**（日期 / 下行 / 上行 / 合计），悬停的柱子会加深；可在**最近 14 天 / 最近 30 天**之间切换
- **下：历史明细表** —— 斑马纹、表头固定、可滚动，列：日期 / 下行 / 上行 / 合计（下行蓝、上行橙、合计加粗），最多展示**一整年**
- 说明：**未引入第三方图表库**——图表是用 GDI+ 手绘的（约 200 行），因此 exe 依然是单文件、无新增依赖，外观完全可控。
  exe 里其实本来就带着 OxyPlot（LibreHardwareMonitor 的依赖，MPL/MIT），但为避免依赖其 API 版本、并保持与浮窗一致的观感，没有使用。

**数据层增强**
- 历史数据**原子写入**：先写 `traffic.dat.tmp` 再替换，避免写一半断电损坏历史
- 新增 `Traffic.RecentAsc(days)`（补零升序，画图用）与 `Traffic.AllDays(max)`（倒序全量，表格用）；历史**不裁剪**，表格最多显示 366 天
- 数据文件路径可用环境变量 `RAYRADAR_TRAFFIC_DATA` 覆盖（自测 / 演示用，不影响正式数据）

## v4.4 — 2026-09-18

**新增**
- **点「网速」块 → 流量统计**：弹出统计窗口，显示 **今天（最近 1 天）/ 最近 30 天 / 最近 1 年** 的下行、上行与合计，另有最近 14 天明细表。
  数据由程序自己**按天累计**（每秒采样网卡增量写入 `%APPDATA%\RayRadar\traffic.dat`）。
  ⚠️ 只能统计 **Ray雷达 运行期间**的流量，从首次运行当天开始累计——Windows 自身不提供按天的历史流量（历史上网量存在 SRUM 的 ESE 数据库里，读取需要管理员且要解析 ESE，不做）。
- **温度折叠（默认开启）**：浮窗上**只显示「主温度」一块**（默认 CPU，可在设置里切换为显卡/热点/主板/硬盘/内存），
  点它就在浮窗**上方**弹出温度浮层，显示**设置里勾选过的其它温度**；再点一次（或点浮层本身）收起。拖动浮窗时浮层跟着走。

**修复**
- 修「点第二次收不起来」：原先鼠标移动（哪怕 1px 抖动）就隐藏浮层，导致点击时浮层已被隐藏、又被重新打开。现在改为拖动时浮层跟随浮窗移动。
- 打开流量统计窗口时自动收起温度浮层。

**说明**
- 直接覆盖旧的 `RayRadar.exe`；设置文件向后兼容（新增 `CollapseTemps` / `MainTemp` 两个键，缺省为「折叠 + 主温度 CPU」）。

## v4.3 — 2026-09-18

**改进**
- **设置窗口瘦身 + 可滚动**：窗口高度从 840px 降到 **560px**，所有设置项放进可滚动内容区——**鼠标滚轮**（指针停在任意控件上都能滚，包括数字框：已屏蔽「滚轮改数值」的行为）或右侧滚动条都可以滚动；「关闭 / 退出 Ray雷达」固定在底部，不随内容滚动
- 版面压缩：三个开关行改成两列排布，行高整体收紧，一屏能看到更多设置项
- 底部说明改为三行：温度来源 / 驱动状态 / 管理员权限说明

**说明**
- 直接覆盖旧的 `RayRadar.exe` 即可，设置文件 `%APPDATA%\RayRadar\settings.ini` 不受影响

## v4.2 — 2026-09-18

首个公开版本。

**新增**
- 显示块扩展到 8 个：CPU 使用率、内存使用率、上传/下载、CPU 温度、显卡温度、显卡热点、主板温度、硬盘温度（每块可单独开关）
- 温度报警：CPU / 显卡 / 显卡热点 / 主板 / 硬盘 / 内存 六项阈值 + **CPU 20 秒温升阈值**（默认 15 °C，用于发现液冷异常：水泵停转、渗漏、冷排风扇停转）
  - 报警方式：置顶弹窗（含液冷排查提示）+ 提示音；同一项 5 分钟内只提醒一次，弹窗冷却 60 秒
- 设置窗口新增「安装/重装温度驱动」按钮与驱动状态显示、「缺驱动时启动提醒」开关
- 命令行参数 `/settings`（启动即开设置窗）、`/getdriver`（只下载驱动安装包用于排查）

**改进**
- **单文件打包**：LibreHardwareMonitor 及 27 个依赖 DLL 全部以资源形式嵌入 exe，运行时由 `AssemblyResolve` 加载，不再需要附带任何 DLL
- **一次点击部署**：exe 内置 `requireAdministrator` 清单，双击即弹 UAC；首次运行自动注册登录计划任务（最高权限运行）并清理旧的注册表自启，之后开机自动启动且不再弹 UAC
- **换机部署**：缺少温度驱动时由程序从官方发布页下载安装（也可用 exe 同目录下的 `PawnIO_setup.exe` 离线安装）
- 设置窗口关闭时保存设置（此前仅在拖动浮窗时保存）
- 温升阈值控件范围修正为 5–60（此前最小 20，会把默认值 15 顶成 20）
- 设置窗口底部布局重排，避免说明文字压住按钮

**修复**
- 温度面板宽度/排布修正，8 块全开时为 355×40，紧贴任务栏上沿时零边距吸附
- **修复「始终置顶」不生效**：WinForms 在窗口显示阶段会再次调整 z 序，把 `WS_EX_TOPMOST` 冲掉，
  而直接调用 `SetWindowPos(hwnd, HWND_TOPMOST, …)` 会返回成功但样式位不变（已实测）。
  现在改为切换 WinForms 的 `TopMost` 属性（先关再开）来真正写入样式位，并每 5 秒核对一次、掉了就补回
  （全屏游戏结束后也能自动恢复置顶）

**备注**
- 温度来源为 LibreHardwareMonitor + PawnIO（已签名驱动）；**不使用**已被微软列入黑名单的 WinRing0
- 出于 GPL-2.0 许可考虑，PawnIO 安装包不随本仓库/Release 分发，见 [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md)
- 本版本之前的内部迭代（v1–v4.1，仅自用、未发布）不再单独列出
